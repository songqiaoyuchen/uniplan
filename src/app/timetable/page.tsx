import Timetable from '../planner/components/timetable';
import TimetableViewer from './TimetableViewer';

export default function TimetablePage() {
  // return <Timetable />;
  return <TimetableViewer />;
}